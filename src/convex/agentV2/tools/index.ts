import { buscarDocumentos } from './documentosV2/buscarDocumentos';

/**
 * Todas las tools del agente v2 exportadas como objeto para config.ts
 */
export const agentV2Tools = {
	buscarDocumentos,
};
